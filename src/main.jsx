import React from 'react'
import ReactDOM from 'react-dom/client'
import SwipeCards from './App.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SwipeCards />
  </React.StrictMode>,
)
